version: '3.8'
services:
  lapland-bot:
    build: .
    container_name: wecom-lapland-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
